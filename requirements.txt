# resemble-enhance's inference runtime deps, pinned WITHOUT gradio (its demo-UI dep, whose
# fastapi-cli chain sends pip into a backtracking storm and isn't needed for serverless inference).
# torch / torchaudio / torchvision come from the CUDA base image.
celluloid>=0.2.0
deepspeed>=0.12.4,<0.15  # cap: 0.15+ uses torch.library.custom_op (needs torch>=2.4); base is 2.1.1
huggingface-hub
librosa>=0.10.1
matplotlib>=3.8.1
numpy>=1.26.2
omegaconf>=2.3.0
pandas>=2.1.3
ptflops>=0.7.1.2
rich>=13.7.0
scipy>=1.11.4
soundfile>=0.12.1
tqdm>=4.66.1
resampy>=0.4.2
tabulate>=0.8.10
runpod
boto3
requests
